import 'package:flutter/material.dart';
class CenterCirculerprogressbar extends StatelessWidget {
  const CenterCirculerprogressbar({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: CircularProgressIndicator(),
    );
  }
}
